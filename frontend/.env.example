# Backend API base URL (without trailing slash)
# For local dev via Vite proxy, leave empty or use relative path
# For production builds, set to your Railway backend URL
VITE_API_URL=http://localhost:8000
